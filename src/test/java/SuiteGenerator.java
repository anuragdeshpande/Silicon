public class SuiteGenerator {

    public static void main(String[] args) {
        generateSuiteXML(args[0]);
    }

    public static void generateSuiteXML(String basePackage) {
/*        ClassGraph graph = new ClassGraph();
        ClassInfoList classesWithAnnotation = graph.whitelistPackages(basePackage).enableAnnotationInfo().scan().getClassesWithAnnotation(AutomatedTest.class.getCanonicalName());
        TestNG testNG = new TestNG();
        XmlSuite xmlSuite = new XmlSuite();*/
        System.out.println("Hello World - !!!!!!!!!!!!!!!!!!!!!!!!!!!");
    }
}
